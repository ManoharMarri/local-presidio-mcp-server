# Presidio MCP Server — requirements.txt
# Pin these for reproducible installs.
# Use pyproject.toml for editable installs: pip install -e ".[dev]"

# MCP framework (pin major version for stability)
fastmcp>=2.0.0,<3.0.0

# Microsoft Presidio
presidio-analyzer>=2.2.0
presidio-anonymizer>=2.2.0

# spaCy NLP backend
spacy>=3.7.0

# Pydantic v2 (required by FastMCP)
pydantic>=2.6.0

# Dev / test dependencies (install with: pip install -r requirements-dev.txt)
# See requirements-dev.txt
